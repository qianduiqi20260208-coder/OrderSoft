工具表

一、
模型表
1、id
2、ATA章节号
3、模型名称

二、
模型版本表
1、id
2、模型id（fk 模型表.id）
3、模型版本

三、
模型加密授权表
1、id
2、模型版本（fk 模型版本表.id)；
3、模型授权（fk 产品授权表.id);

四、
用户负责模型表
1、ID
2、登录用户名(FK)
3、负责模型（FK）

五、
用户表：
1、ID
2、登录用户名（工号）UNIQUE
3、登录密码
4、用户姓名（中文名）
5、用户角色(“SuperUser”,'模型工程师','软件工程师','访客') //多重角色
6、审批身份（是/否）- 后期能改 //多重身份
7、负责模型id（FK）- 后期能改 
8、联系邮箱
9、电话
10、创建时间
11、最后登录时间
12、在线状态
13、最后一次用到的模型


二十、
用户多重身份表
1、ID
2、工号(FK)
3、用户角色
4、流程身份
5、工单ID（FK）

十二、
工单执行人表
1、ID
2、工单ID(FK)
3、执行人ID(FK)
4、流转时间(自动生成)
5、流转原因


六、
问题复现工单表
1、ID
2、工单ID(FK)
4、对应协调单（填单号/NA）；
5、复现内容（文字描述）；
6、复现参考文件（上传附件）；

7、复现现象；
8、备注；

七、
版本迭代工单表
1、ID
2、工单ID(FK)；
4、对应协调单（填单号/NA）；
6、更新内容（文本，后期自动生成UpdateNotes）；
7、封装要求（文本）；
8、接口是否变化（是/否）；

9、升级后模型版本(fk 模型版本表.id）；
10、备注；

八、
封装+发送工单表
1、ID
2、工单ID(FK)
3、对应协调单（填单号/NA）；
4、更新内容（文本，后期自动生成UpdateNotes）；
5、封装要求（文本）；
6、接口是否变化（是/否）；
7、目标客户（文本）(fk)；
9、是否通过CAE平台验证（是/否）；
10、当前版本模型是否有敏感信息（是/否）- 若是（文本记录敏感信息）；

11、升级后模型版本(fk 模型版本表.id）；
12、是否加密（是/否）；
13、外壳号（fk 加密锁表.id)
14、授权ID（fk 产品授权表.id）
15、备注；


九、
交付发送工单表
1、ID
2、工单ID(FK)
3、目标客户（fk 客户表.id）；
4、是否通过CAE平台验证（是/否）；
5、当前版本模型是否有敏感信息（是/否）；

6、是否加密（是/否）；
7、外壳号（fk 加密锁表.id)
8、授权ID（fk 产品授权表.id）
9、备注；

十、
功能开发工单表
1、ID
2、工单ID(FK)
3、功能描述(创建时)（文本）；

4、模型ID（FK）
5、模型版本ID（FK）；
6、功能描述(完成时)（文本）；

十一、
其他工单表
1、ID
2、工单ID(FK)
3、内容描述（文本）；

4、备注；


十三、
操作日志表
1、id
2、用户id（FK）
3、操作类型
4、操作的表格
5、操作的主键id
6、操作前内容
7、操作后内容
8、创建时间

核心表
十四、
工单表字段：
1、id
2、*发起人；(FK)
3、*发起时间；
4、*工单类型（问题复现；版本迭代；直接封装+发送；交付发送；功能开发；其他）
6、模型ID（FK）
7、模型版本ID（FK）
8、工单状态（'草稿','待审批','待分发','进行中','已完成','已退回'）
9、*审批人；（FK）
10、*参考优先级（'紧急','一般'）；
11、*分发人；（FK）
12、*审批时间；
13、 *任务优先级（'紧急','一般'）；
14、 *分发时间；
15、完成时间；








·································································································

十五、
加密锁表
1、ID；
2、外壳号；
3、外壳序列号；
4、备注；
5、状态（出库；入库；损坏；丢失）；

十六、
产品授权表
1、ID；
2、加密锁表（FK 加密锁表.id）；
4、授权ID(2025071010)；
5、加密类型（本地锁/网络锁/软锁授权）；
6、授权开始日期；
7、授权结束日期；
8、授权状态（未授权/授权中/已过期）;
9、备注（填写软锁信息、填写网络锁）；

十七、
客户信息表
1、ID；
2、客户名称;
3、客户设备类型（lab/IPT/FTD/FFS);
2、客户电脑备注（txt）;
3、授权（fk 产品授权表.id）；

十八
加密锁历史信息表
1、ID；
2、加密锁（fk 加密锁表.id）
3、入库时间；
4、出库时间；

十九
加密锁历史信息表
1、ID；
2、加密锁（fk 加密锁表.id）
3、入库时间；
4、出库时间；

二十
复现工单附件信息表
1、ID
2、工单ID（FK)
3、文件保存路径
4、文件名称
5、附件上传时间